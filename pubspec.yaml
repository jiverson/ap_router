name: ap_router
description: Activepoison Routing.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.0.1
homepage: activepoison.com

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  collection: ^1.15.0
  flutter:
    sdk: flutter
  path_to_regexp: ^0.4.0
  quiver: ^3.0.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
